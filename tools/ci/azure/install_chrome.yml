steps:
- script: |
    set -eux -o pipefail
    ./wpt install --channel canary -d . chrome browser
  displayName: 'Install Chrome Canary'
  condition: and(succeeded(), eq(variables['Agent.OS'], 'Darwin'))
