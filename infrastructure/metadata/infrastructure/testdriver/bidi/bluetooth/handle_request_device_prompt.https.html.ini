[handle_request_device_prompt.https.html]
  disabled:
    if product == "safari": @True
  expected:
    if product == "firefox": ERROR

  [accept upon request_device_prompt_updated event]
    expected:
      if product == "firefox": NOTRUN

  [cancel upon request_device_prompt_updated event]
    expected:
      if product == "firefox": NOTRUN
