id	reference	title	flags	links	assertion
[% FOREACH test IN tests.sort('name') %]
[%+ test.file.replace('\.[a-z]+$', '') +%]	
[%- test.references.0.relpath +%]	
[%- test.title FILTER collapse +%]	
[%- test.flags.join(',') +%]	
[%- test.links.join(',').replace(specroot, '') +%]	
[%- test.asserts.join(' ') FILTER collapse +%]
[% END %]

