[set_permission.https.html]
  [Grant Permission for one realm]
    expected:
      if product != "chrome" and product != "chromium": FAIL

  [Deny Permission, omit one realm]
    expected:
      if product != "chrome" and product != "chromium": FAIL
