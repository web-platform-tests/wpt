[webtransport-h3.https.sub.any.serviceworker.html]
  expected:
    if product == "firefox_android": OK
    TIMEOUT
  [WebTransport server should be running and should handle a bidirectional stream]
    expected:
      if product == "firefox_android": FAIL
      TIMEOUT
