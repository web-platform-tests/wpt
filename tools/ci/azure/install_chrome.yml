steps:
- script: |
    ./wpt install --channel canary chrome browser
    ./wpt install --channel canary chrome webdriver
  displayName: 'Install Chrome Canary'
  condition: and(succeeded(), eq(variables['Agent.OS'], 'Darwin'))
